{
  "name": "smite-orchestrator-scripts",
  "version": "1.0.0",
  "description": "SMITE Orchestrator - Cross-platform TypeScript scripts for workflow coordination",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rimraf dist",
    "prebuild": "npm run clean",
    "test": "node dist/state-manager.js get-state"
  },
  "keywords": [
    "smite",
    "orchestrator",
    "workflow",
    "typescript"
  ],
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "ts-node": "^10.9.0",
    "rimraf": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
